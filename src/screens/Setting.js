import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import CircularSlider from '../component/Animated/CircularSlider/CircularSlider'
const Setting = () => {
  return (
    <View style={{ flex:1 }}>
      <CircularSlider/>
    </View>
  )
}

export default Setting

const styles = StyleSheet.create({})